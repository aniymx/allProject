<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.dao.UserDao">
    <resultMap id="userMap" type="com.itheima.domian.User">
        <id property="userId" column="id"></id>
        <result property="userName" column="username"></result>
        <result property="userBirthday" column="birthday"></result>
        <result property="userSex" column="sex"></result>
        <result property="userAddress" column="address"></result>
        <!-- Integer userId;
         String userName;
         Date userBirthday;
         String userSex;
         String userAddress;-->
    </resultMap>
    <select id="findAll" resultMap="userMap">
        select * from USER
    </select>

    <select id="findByQueryVo" resultMap="userMap" parameterType="com.itheima.domian.QueryVo">
        SELECT * FROM USER WHERE username LIKE #{user.userName}
    </select>


    <!--  <select id="findAll" resultType="com.itheima.domian.User">
          SELECT * FROM USER
      </select>
      &lt;!&ndash; //增加
      void insertUser(User user);
      //删除
      void deleteUser(Integer id);
      //改
      void updataUser(User user);&ndash;&gt;
      <insert id="insertUser" parameterType="com.itheima.domian.User">
          <selectKey keyProperty="id" order="AFTER" resultType="int" keyColumn="id">
              SELECT last_insert_id();
          </selectKey>
          INSERT INTO user (username,birthday,sex,address) VALUE (#{username},#{birthday},#{sex},#{address});

      </insert>
      <delete id="deleteUser" parameterType="Integer">
          DELETE FROM USER WHERE id = #{uid};
      </delete>
      <update id="updateUser" parameterType="com.itheima.domian.User">
          UPDATE USER SET username=#{username},birthday=#{birthday},sex=#{sex},address=#{address} where id=#{id};
      </update>
      &lt;!&ndash;//count&ndash;&gt;
      <select id="countUser" resultType="int">
          SELECT COUNT(*) from USER ;
      </select>-->
</mapper>